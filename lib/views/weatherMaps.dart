import 'package:flutter/material.dart';

class road_state extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('road_state'),
          centerTitle: true,
          backgroundColor: Colors.blue.shade400,
        ),
      );
}
